<div class="row">
  <div class="large-12 columns text-center">
    <nav class="pagination" role="navigation">
        {{#if prev}}
            <a class="newer-posts button" href="{{page_url prev}}">&larr; Newer Posts</a>
        {{/if}}
        {{#if next}}
            <a class="older-posts button" href="{{page_url next}}">Older Posts &rarr;</a>
        {{/if}}
    </nav>
  </div>
</div>
